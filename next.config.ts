import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // basePath: "/roi-calculator",
  // assetPrefix: "/roi-calculator/",
  // trailingSlash: true,
};

export default nextConfig;
